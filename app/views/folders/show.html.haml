%body#page-top.index
  / Navigation
  %nav.navbar.navbar-default.navbar-fixed-top
    .container
      / Brand and toggle get grouped for better mobile display
      .navbar-header.page-scroll
        %button.navbar-toggle{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
          %span.sr-only Toggle navigation
          %span.icon-bar
          %span.icon-bar
          %span.icon-bar
        %a.navbar-brand.page-scroll{:href => root_path} UM Bragado
      / Collect the nav links, forms, and other content for toggling
      #bs-example-navbar-collapse-1.collapse.navbar-collapse
        %ul.nav.navbar-nav.navbar-right
          %li.hidden
            %a{:href => "#page-top"}
          %li
            %a{:href => "/"} Home  
          %li
            %a.page-scroll{:href => "#contact"} Contacto



%section#files{style: "background-color: #222; background-image: url('../img/map-image.png'); background-position: center; background-repeat: no-repeat;"}
  .container
    .row
      .col-lg-12
        %h2.section-heading{style: "color: white;"}
          = @folder.full_name

        %br
        %br
        %br

    .row
      - if @files.present?
        - @files.each do |file| 
          - file_path = file.file_type == "folder" ? folder_path(id: file.id) : file.file.url
          .col-md-2.col-sm-3.portfolio-item.text-center
            %a.portfolio-link{href: file_path, style: "color: white; text-decoration: none;"}
              %img.img-responsive{:alt => "", :src => "/img/files/#{file.file_type}.png", style: "width: 175px; height: 150px;"}/
              .portfolio-caption
                %h4.text-center
                  = file.name
      - else
        .col-xs-12
          .admin-common-panel
            .row.admin-common-panel__content
              .col-xs-12
                .col-xs-12
                  %h2.text-center
                    ¡Lo sentimos!
                  %h3.text-center
                    Todavía no se encuentra disponible el material de esta sección. Estamos trabajando para cargar todo el contenido en nuestra página. Volvé pronto por si hay novedades



/ Clients Aside
= render "home/clients_section"

/ Contact Section
= render "home/contact_section"

/ Footer
= render "home/footer"